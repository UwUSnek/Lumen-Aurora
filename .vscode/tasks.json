
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build compiler",
            "command": "./Tools/Build_compiler.sh",
            "args": [],
            "problemMatcher": [ "$gcc" ],
            "options": { "cwd": "${workspaceFolder}" },
            "group": "build",
            "presentation": { "reveal": "always" }
        },
        {
            "type": "shell",
            "label": "Run tests (no debug)",
            "command": "bash",
            "args": [
                "-c",
                "clear && ./Build/out ./Test/main.lum" ],
            "problemMatcher": [],
            "options": { "cwd": "${workspaceFolder}" },
            "group": "build",
            "presentation": { "reveal": "always" }
        }
    ]
}